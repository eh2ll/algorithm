#include <bits/stdc++.h>

using namespace std;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);

    int T; cin >> T;
    assert(T == 5);
    while(T--){
        int N, M; cin >> N >> M;
        cout << N + M << '\n';
    }

    return 0;
}